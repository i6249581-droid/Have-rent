# HavenRent Backend

Deploy this folder as a Node.js Web Service on Render/Railway.

Build command:
npm install

Start command:
npm start

Set these environment variables on the hosting provider:
MONGODB_URI=your MongoDB connection string
JWT_SECRET=your secret

Do NOT upload a real `.env` file to GitHub.
